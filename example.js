/**
 * Example file to demonstrate a pull request
 * 
 * This file shows how you might make changes in a PR.
 */

function calculateSum(a, b) {
  return a + b;
}

// Before PR: Simple greeting
function greet(name) {
  return "Hello " + name;
}

module.exports = { calculateSum, greet };
